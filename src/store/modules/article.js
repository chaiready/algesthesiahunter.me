import { showMessage } from 'utils/showMessage'
import http from 'utils/http'

const state = {}

const mutations = {
  // UPDATE_DEFAULT_USER_ROLE(state, roleName) {
  //   state.user.defaultTeam.roleName = roleName
  // }
}

const actions = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
}
